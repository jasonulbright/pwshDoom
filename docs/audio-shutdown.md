# Finish replay audio before closing

The second full E1M3 Matrix recording completed gameplay but left 23 audio packets unconsumed and cancelled 5,040 queued device frames. Shorter tests had happened to finish with an empty queue. The failed recording and audit are preserved in `results/e1m3-matrix-second-*` and ignored local footage.

A successful replay end now requests stop code 2 from the simulation process. Before stopping its audio worker, the simulation uses the existing bounded drain protocol through the final produced sequence number. The worker consumes pending packets and waits for all submitted device buffers to return, then acknowledges the boundary. The report records `ShutdownAudioDrain`, including the sequence, pending count, QPC timestamps, elapsed wait and completed frames. A three-second drain failure becomes a reported error; it does not silently discard audio and claim success.

Ordinary quit, errors and owner-loss cleanup retain the immediate stop path. The host allows up to ten seconds for the requested graceful process close, covering the existing bounded drain/worker teardown and report writing; other closes retain their five-second bound. Gameplay clocks stop before teardown as before. The drain's separate timing and the original recording retain its actual tail; it is not included as extra gameplay throughput.

`scripts/Test-AudioBackpressure.ps1 -DrainOnClose` uses the real simulation and Windows audio device with two unpaced 150-command bursts and menu/resume between them. This mode removes the prior artificial two-second sleep. `results/audio-shutdown-drain-first.json` passes nine assertions: 31 packets are actually pending when close begins, all 300 ordered packets are consumed, and all 378,000 frames return without cancellation. The observed drain is 974.970 ms through sequence 299. This verifies bounded queue completion and control behavior, not music fidelity or physical listening.

The campaign recording auditor now additionally requires every submitted audio frame to return with zero cancelled queued frames. The third full E1M3 Matrix/music recording passes all **47 integration checks** (`results/e1m3-matrix-third-audit.json`): 7,118 commands, 24 original checkpoints, intermission and E1M4 entry, score transitions, capture integrity and complete movie decoding. All 8,968,680 submitted frames return, with no cancellation or unconsumed packets. The final drain begins with 14 pending packets and waits 482.7021 ms through sequence 7,117. Recorded input, current implementation fingerprint and pre-run source pins agree.

This fixes the ending, not during-play pacing. There are still 77 pre-final queue-empty polling observations; these are not acoustic/hardware underrun measurements. The recorded route averages 29.983 tics/sec and 47.965 console writes/sec over 237.403 active seconds (240.007 wall seconds). The earlier recording averaged 60.001 writes/sec, so the variation is retained as an open performance issue. No sustained 35/60, uninterrupted playback or acoustic-fidelity qualification follows from the drain repair.

The complete original footage and scoped audio remain in ignored `local/recordings/e1m3-matrix-third-*`; a transition excerpt and actual intermission/E1M4 images were exported and visually inspected. Katakana and the destination HUD are present; intermission lettering remains too dark. Raw JSON receipts were copied byte-identically to `results/`; `e1m3-matrix-third-timing.json` retains the sequence-aligned audio analysis. No human listening review is claimed.
